---
phase: 01-capture-ingest-spine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/webhook/index.ts
  - src/webhook/bot.ts
  - src/shared/instagram.ts
  - src/db/queries.ts
  - test/instagram.test.ts
autonomous: false
requirements: [CAP-01, CAP-02, CAP-03, CAP-04]
user_setup:
  - service: telegram
    why: "A bot token must be created by a human via BotFather"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram → @BotFather → /newbot → copy token"
      - name: TELEGRAM_WEBHOOK_SECRET
        source: "Self-chosen random string; set as a Wrangler secret and used to verify webhook calls"
    dashboard_config:
      - task: "Register the webhook"
        location: "After deploy"
        details: "Call Telegram setWebhook with the deployed Worker URL and the secret_token (README documents the exact curl/wrangler command)"
must_haves:
  truths:
    - "Sending a reel URL to the bot creates a queued submission and the bot ACKs"
    - "Sending a video file (no URL) stores the file in R2 and creates a queued submission"
    - "Re-sending the same reel URL is detected as a duplicate and not re-queued"
  artifacts:
    - path: src/webhook/bot.ts
      provides: "grammY bot handlers for URL and video messages"
      contains: "grammy"
    - path: src/shared/instagram.ts
      provides: "Reel shortcode parser"
      exports: ["parseReelShortcode"]
    - path: src/db/queries.ts
      provides: "submission insert/dedupe/status helpers"
      exports: ["insertSubmission", "findSubmissionByShortcode"]
  key_links:
    - from: src/webhook/bot.ts
      to: INGEST_QUEUE
      via: "queue.send(JobMessage) after creating submission"
      pattern: "\\.send\\("
    - from: src/webhook/bot.ts
      to: src/db/queries.ts
      via: "dedupe + insert before enqueue"
      pattern: "findSubmissionByShortcode|insertSubmission"
---

<objective>
Build the capture entry point: a grammY Telegram bot running on a Worker (webhook mode) that accepts a reel link or an uploaded video file, dedupes by shortcode, persists a submission, stores any uploaded file in R2, enqueues a job, and immediately acknowledges the user.

Purpose: Delivers the "forward a reel, get an instant ACK" experience and produces the queue jobs the ingest Container (01-03) consumes.
Output: Webhook Worker + bot handlers, an Instagram shortcode parser (unit-tested), and D1 query helpers.
</objective>

<execution_context>
@/home/user/insta-code/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/insta-code/.claude/get-shit-done/templates/summary.md
@/home/user/insta-code/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-capture-ingest-spine/01-CONTEXT.md
@.planning/phases/01-capture-ingest-spine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instagram shortcode parser (unit-tested)</name>
  <files>src/shared/instagram.ts, test/instagram.test.ts</files>
  <read_first>src/shared/types.ts</read_first>
  <action>Implement `parseReelShortcode(text: string): string | null` that extracts the shortcode from instagram.com/reel/{code}/, /reels/{code}/, and /p/{code}/ URLs (with or without query strings like ?igsh=...), and returns null when no IG URL is present. Also export `extractFirstUrl(text)`. Write `bun test` cases covering the three example reel URLs from CONTEXT (DWpSK4uDhIO, DYeHzvgCURl, DVbfcdTkZ7R), trailing query params, and a no-URL message.</action>
  <verify>`bun test test/instagram.test.ts` passes</verify>
  <acceptance_criteria>
    - parseReelShortcode("https://www.instagram.com/reel/DWpSK4uDhIO/?igsh=x") returns "DWpSK4uDhIO"
    - parseReelShortcode("hello") returns null
  </acceptance_criteria>
  <done>Parser tests pass</done>
</task>

<task type="auto">
  <name>Task 2: D1 query helpers with dedupe</name>
  <files>src/db/queries.ts</files>
  <read_first>src/db/migrations/0001_init.sql, src/shared/types.ts</read_first>
  <action>Implement helpers over the D1 binding: `insertSubmission(db, {...})` returning the new id; `findSubmissionByShortcode(db, shortcode)` for URL dedupe; `findSubmissionByHash(db, hash)` for file dedupe; `setSubmissionStatus(db, id, status, error?)`. Use parameterized statements only (no string interpolation) to avoid SQL injection.</action>
  <verify>`bunx tsc --noEmit` passes; manual local insert via wrangler d1 execute works</verify>
  <acceptance_criteria>
    - queries.ts uses bound parameters (`.bind(`), never template-string SQL values
    - exports insertSubmission and findSubmissionByShortcode
  </acceptance_criteria>
  <done>Helpers compile and run against local D1</done>
</task>

<task type="auto">
  <name>Task 3: Webhook Worker + grammY handlers</name>
  <files>src/webhook/index.ts, src/webhook/bot.ts</files>
  <read_first>src/shared/instagram.ts, src/db/queries.ts, src/shared/types.ts, wrangler.toml</read_first>
  <action>index.ts is the Worker fetch handler: verify the Telegram secret_token header against TELEGRAM_WEBHOOK_SECRET (reject otherwise), then hand the update to the grammY bot via webhookCallback. bot.ts wires handlers: (1) text/message containing an IG reel URL → parse shortcode → dedupe via findSubmissionByShortcode → if new, insertSubmission(status='queued') and INGEST_QUEUE.send({submissionId, sourceUrl}) then reply "Got it — processing…"; if duplicate, reply "Already captured ✓". (2) video/document message → download the Telegram file, put bytes in R2 under an uploads/ prefix, compute a content hash for dedupe, insertSubmission(status='queued', uploadedFileKey, content_hash) and enqueue {submissionId, uploadedFileKey}, reply "Got your file — processing…". Keep handlers small; all IO via the bound env (DB, MEDIA, INGEST_QUEUE).</action>
  <verify>`bunx tsc --noEmit` passes; `bunx wrangler dev` boots; posting a sample Telegram update JSON to the local endpoint with the secret header creates a row in local D1 and a queue send (observe via wrangler dev logs)</verify>
  <acceptance_criteria>
    - index.ts rejects requests whose secret_token header != TELEGRAM_WEBHOOK_SECRET
    - bot.ts calls INGEST_QUEUE.send after insertSubmission for new URLs
    - duplicate URL path replies without enqueuing
  </acceptance_criteria>
  <done>Local webhook creates submissions, dedupes, and enqueues</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Capture Worker ready to receive Telegram webhooks</what-built>
  <how-to-verify>Create a bot with @BotFather, set TELEGRAM_BOT_TOKEN + TELEGRAM_WEBHOOK_SECRET as Wrangler secrets, deploy, and register the webhook (README has the exact command). Then forward one of the example reels to the bot from your phone and confirm you get an instant "processing…" reply and a row appears in D1.</how-to-verify>
  <resume-signal>Type "captured" once you've seen the ACK + D1 row, or describe what failed</resume-signal>
</task>

</tasks>

<verification>
- [ ] `bun test` passes (shortcode parser)
- [ ] `bunx tsc --noEmit` passes
- [ ] Local webhook creates a queued submission and enqueues a job; duplicates are skipped
</verification>

<success_criteria>
- All tasks completed
- Forwarding a reel link or a file to the bot yields an ACK, a queued submission, and a queue job; duplicates are detected
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-ingest-spine/01-02-SUMMARY.md`
</output>
