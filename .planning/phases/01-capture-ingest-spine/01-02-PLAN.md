---
phase: 01-capture-ingest-spine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/intake/server.ts
  - src/intake/watcher.ts
  - src/index.ts
  - test/server.test.ts
  - test/watcher.test.ts
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04, ING-02, OPS-01]
user_setup: []

must_haves:
  truths:
    - "Posting a reel URL to the localhost HTTP endpoint creates a deduped submission + queued job"
    - "Dropping a video file into the watched folder creates a file-source submission + queued job once the file is fully written"
    - "Dropped files are deduped by content hash; the same file dropped twice is processed once"
    - "Submitting an already-seen reel shortcode via HTTP returns a duplicate response without enqueuing"
    - "The HTTP endpoint binds to 127.0.0.1 only (never 0.0.0.0)"
    - "A single `bun run worker` process starts the worker loop, the HTTP intake, and the drop-folder watcher together (always-on service)"
  artifacts:
    - path: "src/intake/server.ts"
      provides: "Bun.serve localhost-only HTTP intake (POST url, POST/multipart file) -> submission + enqueue"
      contains: "Bun.serve"
    - path: "src/intake/watcher.ts"
      provides: "fs.watch drop-folder watcher with debounce + stable-size probe -> content-hash dedup -> submission + enqueue"
      contains: "fs.watch"
    - path: "src/index.ts"
      provides: "always-on service entry: starts worker loop + HTTP server + watcher"
  key_links:
    - from: "src/intake/server.ts"
      to: "src/queue/queue.ts"
      via: "enqueue() after inserting submission"
      pattern: "enqueue\\("
    - from: "src/intake/watcher.ts"
      to: "src/queue/queue.ts"
      via: "enqueue() after stable-size + hash dedup"
      pattern: "enqueue\\("
    - from: "src/index.ts"
      to: "src/worker/loop.ts"
      via: "starts the worker poll loop"
      pattern: "loop|runWorkerLoop"
---

<objective>
Add the two remaining always-on capture surfaces on top of the spine from 01-01: a localhost-only HTTP intake endpoint (CAP-01) and a watched drop-folder for video files (CAP-02, the load-bearing path and the ING-02 fallback). Both feed the SAME submissions/jobs queue the worker already drains. Then wire a single service entry (`src/index.ts`) that runs the worker loop + HTTP server + watcher in one process, so the user starts the whole system with one command (OPS-01).

Purpose: Complete the capture topology (drop-folder + URL intake) feeding one queue, so a user can drop a file OR hit a local endpoint and walk away. Honors D-04, D-05, D-06, D-07, D-10.
Output: HTTP intake server, drop-folder watcher, unified service entry, and tests for both intake paths.
</objective>

<execution_context>
@/home/user/insta-code/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/insta-code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-capture-ingest-spine/01-CONTEXT.md
@.planning/phases/01-capture-ingest-spine/01-RESEARCH.md
@.planning/phases/01-capture-ingest-spine/01-01-SUMMARY.md
@src/shared/instagram.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Localhost-only HTTP intake endpoint (URL + file upload) with dedup</name>
  <files>src/intake/server.ts, test/server.test.ts</files>
  <read_first>
    - /home/user/insta-code/src/intake/cli.ts (the validated submit logic from 01-01 — reuse the same validate+insert+enqueue path; do NOT build a second pipeline)
    - /home/user/insta-code/src/queue/queue.ts and src/db/db.ts (from 01-01)
    - /home/user/insta-code/src/shared/instagram.ts (parseReelShortcode for URL validation)
    - /home/user/insta-code/src/shared/config.ts (HTTP_PORT, MEDIA_DIR, DROP_DIR)
    - /home/user/insta-code/.planning/phases/01-capture-ingest-spine/01-RESEARCH.md (Security Domain: bind 127.0.0.1; CAP-01/CAP-04 rows; Recommended Project Structure)
    - /home/user/insta-code/.planning/phases/01-capture-ingest-spine/01-CONTEXT.md (D-04, D-06, D-07; HTTP shape is Claude's discretion)
  </read_first>
  <behavior>
    - server.test.ts (start server on 127.0.0.1 with a temp DB): POST a reel URL (JSON `{url}`) -> 200/202 with a submission id, and a jobs row is pending. POST the SAME url -> a duplicate response (e.g. 200 with `{duplicate:true}`) and NO second job. POST a non-instagram URL -> 400 and nothing inserted. POST a multipart video file -> 200 with a submission id, the file persisted under MEDIA_DIR/DROP_DIR, source_type='file', and a job enqueued. The bound address is 127.0.0.1 (assert the server hostname/options, not 0.0.0.0).
  </behavior>
  <action>
    src/intake/server.ts: `Bun.serve({ hostname: "127.0.0.1", port: config.HTTP_PORT, fetch })` — hostname MUST be `127.0.0.1`, never `0.0.0.0` (Security: V4 access control / LAN exposure). Factor the URL submit logic shared with cli.ts into a reusable `submitUrl(url)` helper (extract from 01-01's cli or a small shared intake module) so HTTP and CLI use ONE validated path (validate via parseReelShortcode -> reject non-reel URLs with 400; INSERT ... ON CONFLICT(reel_shortcode) DO NOTHING; enqueue only if newly inserted — CAP-04/D-07). Routes (shape is Claude's discretion per D-04): a JSON `POST /submit {url}` and a `POST /upload` accepting a multipart video file. For uploads: read the multipart file, `path.basename()` the provided filename and confine the write under MEDIA_DIR (Security: path traversal — reject names containing `..` or path separators), compute a SHA-256 content hash (crypto.subtle), INSERT a submission with source_type='file', file_path, content_hash using `ON CONFLICT(content_hash) DO NOTHING` (CAP-04 file dedup / D-07), enqueue if new. Return JSON with submission id + status, or a duplicate marker. Log receipt (D-06/CAP-03). Reject oversized/non-video uploads with 4xx.
  </action>
  <verify>
    <automated>cd /home/user/insta-code && bun run typecheck && bun test test/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - server.test.ts passes: URL submit creates a pending job; duplicate URL returns a duplicate response with no second job; non-instagram URL returns 400 and inserts nothing; file upload creates a source_type='file' submission + job and persists the file under MEDIA_DIR.
    - `grep -n "0.0.0.0" src/intake/server.ts` returns nothing AND `grep -n "127.0.0.1" src/intake/server.ts` matches the Bun.serve hostname (Security: bind localhost only).
    - server.ts reuses the same validate+insert+enqueue helper as cli.ts (no duplicated/divergent intake logic) — confirm a shared `submitUrl` import or helper.
    - Upload path uses `path.basename` and rejects `..` filenames (path-traversal guard).
  </acceptance_criteria>
  <done>A localhost-only HTTP endpoint accepts reel URLs and uploaded video files, dedups them, and enqueues jobs through the same queue as the CLI (CAP-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop-folder watcher (fs.watch + debounce + stable-size) with content-hash dedup</name>
  <files>src/intake/watcher.ts, test/watcher.test.ts</files>
  <read_first>
    - /home/user/insta-code/.planning/phases/01-capture-ingest-spine/01-RESEARCH.md (§4 fs.watch + debounce + stable-size probe pattern; Pitfall 6 event bursts; chokidar fallback note)
    - /home/user/insta-code/src/queue/queue.ts, src/db/db.ts, src/shared/config.ts (DROP_DIR, MEDIA_DIR)
    - /home/user/insta-code/src/intake/server.ts (Task 1 — reuse the file-submission helper: hash + insert source_type='file' + enqueue)
    - /home/user/insta-code/.planning/phases/01-capture-ingest-spine/01-CONTEXT.md (D-04, D-07; watcher impl + debounce is Claude's discretion)
  </read_first>
  <behavior>
    - watcher.test.ts (point watcher at a temp dir + temp DB): write a small video file into the watched dir; after the debounce + stable-size window the watcher creates exactly ONE source_type='file' submission + job (Pitfall 6: event bursts must not double-enqueue). Writing a file with a non-video extension creates nothing. Dropping the same content twice (same hash) yields one processed submission (CAP-04). A partially-written file (size still changing) is NOT enqueued until stable.
  </behavior>
  <action>
    src/intake/watcher.ts: implement `watchDropFolder(dir, onFile)` exactly per RESEARCH §4 — `fs.watch(dir, cb)`, filter to video extensions (.mp4/.mov/.webm/.mkv), per-filename debounce (1s timer reset on each event — Pitfall 6), then a stable-size probe (`stat`, `Bun.sleep(500)`, `stat` again; enqueue only if size unchanged and > 0). On a stable file: `path.basename` + confine under DROP_DIR (Security: path traversal), compute SHA-256 content hash, reuse the file-submission helper from Task 1 (INSERT source_type='file', file_path, content_hash ON CONFLICT(content_hash) DO NOTHING; enqueue if new). Catch and log per-file errors so one bad file never kills the watcher (D-06/OPS-04). Keep chokidar as a documented swap-in only if fs.watch proves unreliable on the host (RESEARCH §4 / Assumption A2) — do NOT add it now. The file then flows through the SAME worker (ING-02 fallback branch already exists in process.ts).
  </action>
  <verify>
    <automated>cd /home/user/insta-code && bun run typecheck && bun test test/watcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - watcher.test.ts passes: a fully-written video file yields exactly ONE submission+job; a burst of fs.watch events for one file does not double-enqueue (Pitfall 6); a non-video file is ignored; identical content dropped twice is deduped (CAP-04); a still-growing file is not enqueued until size stabilizes.
    - watcher.ts uses fs.watch + a per-filename debounce timer + a two-stat stable-size probe (RESEARCH §4).
    - watcher.ts reuses the Task 1 file-submission helper (no divergent file-intake path).
  </acceptance_criteria>
  <done>Dropping a video file into the watched folder reliably produces one deduped, queued job — the load-bearing capture path and the link-download fallback (CAP-02 / ING-02).</done>
</task>

<task type="auto">
  <name>Task 3: Unified always-on service entry (worker + HTTP + watcher in one process)</name>
  <files>src/index.ts</files>
  <read_first>
    - /home/user/insta-code/src/worker/loop.ts (the worker poll loop from 01-01)
    - /home/user/insta-code/src/intake/server.ts and src/intake/watcher.ts (Tasks 1-2)
    - /home/user/insta-code/src/shared/config.ts (DROP_DIR, HTTP_PORT)
    - /home/user/insta-code/package.json (the `worker` script — repoint it to src/index.ts)
    - /home/user/insta-code/.planning/phases/01-capture-ingest-spine/01-CONTEXT.md (D-03: long-lived process + thin intake; process supervision is the host's concern)
  </read_first>
  <action>
    src/index.ts: the single always-on service entry (D-03/OPS-01). On start: run DB migration/orphan-recovery, start `Bun.serve` HTTP intake (from server.ts), start `watchDropFolder(DROP_DIR, ...)` (from watcher.ts), and run the worker poll loop (from loop.ts) concurrently in ONE Bun process. Log a startup banner listing the bound HTTP address (127.0.0.1:PORT), the watched DROP_DIR, and the ffmpeg/yt-dlp probe results, then which capture paths are active. Handle SIGINT/SIGTERM for a clean shutdown (stop accepting, let the in-flight job's lease expire). Update package.json `worker` script to `bun run src/index.ts` (it temporarily pointed at loop.ts in 01-01). Process supervision (systemd/pm2) is explicitly the user's host concern — do not build it (D-03).
  </action>
  <verify>
    <automated>cd /home/user/insta-code && bun run typecheck && timeout 6 bun run src/index.ts 2>&1 | grep -iE "127.0.0.1|watching|worker" || true</automated>
  </verify>
  <acceptance_criteria>
    - `bun run src/index.ts` starts and logs a banner showing the 127.0.0.1 HTTP bind, the watched DROP_DIR, and worker poll start — all three subsystems active in one process (OPS-01).
    - package.json `worker` script points at `src/index.ts`.
    - The process responds to SIGINT/SIGTERM without leaving an orphaned `processing` job that can't recover (recoverOrphaned handles restart).
    - USER-HOST ONLY: with ffmpeg installed + Chrome closed, dropping a fixture .mp4 into DROP_DIR while the service runs produces a findings row (end-to-end via the watcher path). Surfaced as the end-of-phase human-check.
  </acceptance_criteria>
  <done>One command (`bun run worker`) runs the worker, the localhost HTTP intake, and the drop-folder watcher together as the always-on local service (OPS-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network/localhost client -> HTTP intake | submitted URLs and uploaded files cross into the app from a local HTTP client |
| filesystem (dropped filenames) -> DROP_DIR/MEDIA_DIR writes | externally-named dropped/uploaded files cross into local path joins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | HTTP intake (server.ts) | mitigate | `Bun.serve({ hostname: "127.0.0.1" })` — bind localhost only, never 0.0.0.0 (Task 1 grep gate) |
| T-02-02 | Tampering | URL submit (server.ts) | mitigate | Same parseReelShortcode validation as CLI — reject non-reel URLs with 400 before yt-dlp ever sees them |
| T-02-03 | Tampering | file upload + drop-folder names (server.ts, watcher.ts) | mitigate | `path.basename` + confine under MEDIA_DIR/DROP_DIR; reject `..`/separators (path traversal) |
| T-02-04 | Denial of Service | file upload size / fs.watch bursts | mitigate | Reject oversized/non-video uploads (4xx); per-filename debounce + stable-size probe + hash dedup prevent double-enqueue |
| T-02-05 | Information Disclosure | startup/receipt logs | mitigate | Log addresses/paths only; never log cookies or env wholesale (reuse 01-01 config redaction) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; chokidar/readdirp already present + slopcheck [OK] (RESEARCH Package Legitimacy Audit), added only if fs.watch proves unreliable |
</threat_model>

<verification>
- `bun test test/server.test.ts test/watcher.test.ts` green.
- HTTP server binds 127.0.0.1 only (grep gate); URL + file intake both dedup and enqueue through the shared path.
- `bun run src/index.ts` starts worker + HTTP + watcher in one process.
- USER-HOST: dropping a fixture video produces a findings row end-to-end (end-of-phase human-check).
</verification>

<success_criteria>
- A reel URL POSTed to the localhost endpoint and a video dropped in the watched folder both become deduped, queued jobs (CAP-01, CAP-02, CAP-04).
- Dropped files serve as the link-download fallback (ING-02) via the existing worker branch.
- One command runs the always-on local service: worker + HTTP intake + watcher (OPS-01).
- The HTTP endpoint is bound to localhost only.
</success_criteria>

<output>
Create `.planning/phases/01-capture-ingest-spine/01-02-SUMMARY.md` when done.
</output>
