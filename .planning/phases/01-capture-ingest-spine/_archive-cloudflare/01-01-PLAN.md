---
phase: 01-capture-ingest-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wrangler.toml
  - package.json
  - tsconfig.json
  - src/shared/types.ts
  - src/db/schema.sql
  - src/db/migrations/0001_init.sql
  - .dev.vars.example
  - README.md
autonomous: false
requirements: [OPS-01, OPS-02, OPS-03, KB-01]
user_setup:
  - service: cloudflare
    why: "Account, Wrangler auth, and provisioning D1/R2/Queues require human login"
    env_vars:
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "Cloudflare dashboard → Workers & Pages → Account ID"
    dashboard_config:
      - task: "Authenticate Wrangler"
        location: "Terminal"
        details: "Run `bunx wrangler login` once so subsequent `wrangler` commands can provision resources"
must_haves:
  truths:
    - "A Wrangler project builds/type-checks and defines bindings for D1, R2, and a Queue"
    - "D1 has submissions and findings tables created by a migration"
    - "A shared job-message type and Finding type are defined and importable"
  artifacts:
    - path: wrangler.toml
      provides: "Worker config with D1, R2, Queue (producer+consumer), and Container bindings"
      contains: "queues"
    - path: src/db/migrations/0001_init.sql
      provides: "submissions + findings schema"
      contains: "create table"
    - path: src/shared/types.ts
      provides: "JobMessage and Finding TypeScript types"
      exports: ["JobMessage", "Finding", "SubmissionStatus"]
  key_links: []
---

<objective>
Create the all-Cloudflare project skeleton and the persistence model that every later plan builds on: a Wrangler project with D1 + R2 + Queue (and a placeholder Container binding), the D1 schema (submissions + findings), and shared TypeScript types for the job message and Finding record.

Purpose: Establishes the bindings, data model, and job contract so the capture Worker (01-02) and the ingest Container/consumer (01-03) can be built against a stable interface.
Output: wrangler.toml, package.json/tsconfig, D1 schema + migration, shared types, env example, and a short README.
</objective>

<execution_context>
@/home/user/insta-code/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/insta-code/.claude/get-shit-done/templates/summary.md
@/home/user/insta-code/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-ingest-spine/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Wrangler + Bun TypeScript project</name>
  <files>package.json, tsconfig.json, wrangler.toml, .dev.vars.example, README.md</files>
  <read_first>.planning/phases/01-capture-ingest-spine/01-CONTEXT.md</read_first>
  <action>Create a Wrangler (workerd) TypeScript project managed with Bun. package.json scripts use `wrangler` via `bunx` (dev/deploy/migrations). Add devDeps: wrangler, typescript, @cloudflare/workers-types, grammY (for 01-02). tsconfig targets ES2022/modules with workers-types. In wrangler.toml declare: main worker entry, compatibility_date, a D1 binding (DB), an R2 bucket binding (MEDIA), a Queue producer binding (INGEST_QUEUE) and a consumer config for the same queue with a dead-letter queue, and a placeholder Container/Durable Object binding for the ingest service to be implemented in 01-03 (commented with a TODO if the exact Container binding syntax is finalized there). Add .dev.vars.example listing TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET (used in 01-02). README documents prerequisites and the `wrangler login` + resource-provisioning commands.</action>
  <verify>`bunx tsc --noEmit` passes; `bunx wrangler deploy --dry-run` parses wrangler.toml without binding errors (resources may not exist yet — dry run only)</verify>
  <acceptance_criteria>
    - wrangler.toml contains a `[[d1_databases]]`, an `[[r2_buckets]]`, and a `[[queues.producers]]` + `[[queues.consumers]]` entry
    - package.json scripts invoke `wrangler` through `bunx`
    - .dev.vars.example contains TELEGRAM_BOT_TOKEN and TELEGRAM_WEBHOOK_SECRET
  </acceptance_criteria>
  <done>Project type-checks and wrangler.toml parses</done>
</task>

<task type="auto">
  <name>Task 2: Define D1 schema and shared types</name>
  <files>src/db/schema.sql, src/db/migrations/0001_init.sql, src/shared/types.ts</files>
  <read_first>.planning/phases/01-capture-ingest-spine/01-CONTEXT.md</read_first>
  <action>Create the migration 0001_init.sql with two tables. `submissions`: id (pk), telegram_chat_id, telegram_message_id, source_url (nullable), reel_shortcode (nullable, indexed unique-when-present), uploaded_file_key (nullable R2 key), content_hash (nullable, for file dedupe), status (text: 'queued'|'processing'|'done'|'failed'), error (nullable), created_at, updated_at. `findings`: id (pk), submission_id (fk), reel_shortcode (indexed), author_handle, caption, posted_at (nullable), duration_sec (nullable), media_key (R2), audio_key (R2), keyframe_keys (json array of R2 keys), status, created_at, updated_at. Keep schema.sql as the canonical current schema mirroring the migration. In src/shared/types.ts export: SubmissionStatus union, Submission, Finding interfaces, and JobMessage = { submissionId: string; sourceUrl?: string; uploadedFileKey?: string } — the contract carried over the Queue.</action>
  <verify>Apply migration to a local D1: `bunx wrangler d1 migrations apply DB --local` succeeds and creates both tables (`bunx wrangler d1 execute DB --local --command "select name from sqlite_master where type='table'"` lists submissions and findings)</verify>
  <acceptance_criteria>
    - 0001_init.sql contains `create table` for submissions and findings
    - types.ts exports JobMessage, Finding, SubmissionStatus
    - keyframe_keys stored as JSON text column
  </acceptance_criteria>
  <done>Local D1 migration applies and tables exist</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Wrangler project with D1/R2/Queue config and an initial migration</what-built>
  <how-to-verify>Run `bunx wrangler login`, then provision remote resources (the README lists exact commands): create the D1 database, R2 bucket, and Queue (+ dead-letter queue), and paste the generated IDs into wrangler.toml. This requires your Cloudflare account and cannot be automated.</how-to-verify>
  <resume-signal>Type "provisioned" once resource IDs are in wrangler.toml (or "skip" to stay local-only for now)</resume-signal>
</task>

</tasks>

<verification>
- [ ] `bunx tsc --noEmit` passes
- [ ] `bunx wrangler d1 migrations apply DB --local` creates submissions + findings
- [ ] wrangler.toml declares D1, R2, and Queue producer + consumer bindings
</verification>

<success_criteria>
- All tasks completed
- Project builds, types are importable, and the data model + job contract exist for 01-02 and 01-03
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-ingest-spine/01-01-SUMMARY.md`
</output>
