---
phase: 02-understand-transcribe-see
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/0002_enrichment.sql
  - src/db/schema.sql
  - src/shared/dto.ts
  - src/shared/types.ts
  - src/db/queries.ts
  - src/consumer/index.ts
  - src/index.ts
  - src/enrich/index.ts
  - wrangler.toml
autonomous: true
requirements: [TRX-03, VIS-03]
must_haves:
  truths:
    - "findings has enrichment columns and a separate reel-enrich queue exists"
    - "ingest consumer enqueues an enrich job after the Finding is persisted"
    - "the Worker queue() routes reel-ingest vs reel-enrich"
  artifacts:
    - path: src/db/migrations/0002_enrichment.sql
      provides: "enrichment columns on findings"
      contains: "enrich_status"
    - path: src/enrich/index.ts
      provides: "enrich queue consumer skeleton"
      contains: "handleEnrichBatch"
  key_links:
    - from: src/consumer/index.ts
      to: ENRICH_QUEUE
      via: "send EnrichJob after upsertFinding"
      pattern: "ENRICH_QUEUE.send"
---

<objective>
Add the enrichment data model, the `reel-enrich` queue, and the pipeline wiring that hands a freshly
ingested Finding off to a decoupled enrichment consumer.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/02-understand-transcribe-see/02-CONTEXT.md
@.planning/phases/01-capture-ingest-spine/01-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enrichment schema + DTOs + Env</name>
  <action>Migration 0002 adds transcript*, visual_summary, onscreen_text, enrich_status, enriched_at to findings (mirror schema.sql). dto.ts: EnrichJob, TranscriptSegment, TranscriptResult, VisionResult; extend Finding. types.ts Env: ENRICH_QUEUE, GROQ_API_KEY, ANTHROPIC_API_KEY, CLAUDE_MODEL.</action>
  <verify>bun run db:migrate:local; bunx tsc -p tsconfig.json</verify>
  <done>Columns exist; types compile</done>
</task>
<task type="auto">
  <name>Task 2: Queue config + routing + consumer skeleton + queries</name>
  <action>wrangler.toml: reel-enrich producer/consumer + DLQ + CLAUDE_MODEL var. queries.ts: getFinding, getFindingIdBySubmission, setEnrichStatus, updateFindingTranscript, updateFindingVision, markEnriched. consumer/index.ts: enqueue ENRICH_QUEUE after upsert (rename to handleIngestBatch). index.ts: route queue() by batch.queue. enrich/index.ts: handleEnrichBatch with retry/DLQ.</action>
  <verify>bunx wrangler deploy --dry-run --containers-rollout=none shows ENRICH_QUEUE</verify>
  <done>Binding recognized; routing in place</done>
</task>
</tasks>

<success_criteria>
- Migration applies; Worker + container type-check; ENRICH_QUEUE bound; ingest hands off to enrich.
</success_criteria>

<output>
After completion, create `.planning/phases/02-understand-transcribe-see/02-01-SUMMARY.md`
</output>
