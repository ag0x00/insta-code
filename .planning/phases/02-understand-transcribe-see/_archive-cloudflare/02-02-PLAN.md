---
phase: 02-understand-transcribe-see
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/enrich/parse.ts
  - src/enrich/transcribe.ts
  - src/enrich/index.ts
  - test/transcribe.test.ts
autonomous: true
requirements: [TRX-01, TRX-02, TRX-03]
user_setup:
  - service: groq
    why: "Hosted Whisper transcription needs an API key"
    env_vars:
      - name: GROQ_API_KEY
        source: "console.groq.com → API Keys"
must_haves:
  truths:
    - "audio is fetched from R2 and transcribed via Groq verbose_json"
    - "transcript, language, and segments are stored on the finding"
  artifacts:
    - path: src/enrich/transcribe.ts
      provides: "Groq transcription"
      contains: "audio/transcriptions"
    - path: src/enrich/parse.ts
      provides: "pure verbose_json parser"
      exports: ["parseGroqVerboseJson"]
  key_links:
    - from: src/enrich/transcribe.ts
      to: updateFindingTranscript
      via: "persist transcript"
      pattern: "updateFindingTranscript"
---

<objective>
Transcribe the finding's audio with Groq Whisper (timestamps + language) and store it.
</objective>

<context>
@.planning/phases/02-understand-transcribe-see/02-CONTEXT.md
@.planning/phases/02-understand-transcribe-see/02-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Pure parser + tests</name>
  <action>parse.ts: parseGroqVerboseJson(body) -> {text, language, segments[]}. test/transcribe.test.ts covers parsing + missing fields.</action>
  <verify>bun test</verify>
  <done>Parser tests pass</done>
</task>
<task type="auto">
  <name>Task 2: Groq transcription + wire-in</name>
  <action>transcribe.ts: MEDIA.get(audio_key) -> multipart POST to Groq (whisper-large-v3, verbose_json) -> parse -> updateFindingTranscript. Call from enrich/index.ts.</action>
  <verify>bunx tsc</verify>
  <done>Transcription wired into the enrich consumer</done>
</task>
</tasks>

<success_criteria>
- Parser unit-tested; transcript/language/segments persisted on the finding.
</success_criteria>

<output>
After completion, create `.planning/phases/02-understand-transcribe-see/02-02-SUMMARY.md`
</output>
