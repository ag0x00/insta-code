---
phase: 02-understand-transcribe-see
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/enrich/parse.ts
  - src/enrich/vision.ts
  - src/enrich/index.ts
  - test/vision.test.ts
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]
user_setup:
  - service: anthropic
    why: "Claude vision analysis needs an API key"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → API Keys"
must_haves:
  truths:
    - "keyframes are read from R2 and analyzed by Claude with prompt caching"
    - "visual_summary and onscreen_text are stored on the finding"
    - "user gets a final 'Understood' notification after enrichment"
  artifacts:
    - path: src/enrich/vision.ts
      provides: "Claude vision over keyframes"
      contains: "anthropic.com/v1/messages"
    - path: src/enrich/parse.ts
      provides: "pure Claude response parser"
      exports: ["parseClaudeVision"]
  key_links:
    - from: src/enrich/vision.ts
      to: updateFindingVision
      via: "persist visual analysis"
      pattern: "updateFindingVision"
---

<objective>
Analyze the finding's keyframes with Claude (visual summary + on-screen text) and store the result;
notify the user when the finding is fully understood.
</objective>

<context>
@.planning/phases/02-understand-transcribe-see/02-CONTEXT.md
@.planning/phases/02-understand-transcribe-see/02-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Pure parser + tests</name>
  <action>parse.ts: parseClaudeVision(body) -> {visual_summary, onscreen_text}; toBase64. test/vision.test.ts covers clean JSON, code fences, and non-JSON fallback.</action>
  <verify>bun test</verify>
  <done>Parser tests pass</done>
</task>
<task type="auto">
  <name>Task 2: Claude vision + wire-in + notify</name>
  <action>vision.ts: MEDIA.get keyframes -> base64 image blocks -> Anthropic Messages (CLAUDE_MODEL, system prompt with cache_control ephemeral) -> parse -> updateFindingVision. enrich/index.ts runs transcribe + vision concurrently, markEnriched, then notify "🧠 Understood".</action>
  <verify>bunx tsc; wrangler dry-run</verify>
  <done>Vision wired in; finding fully enriched + user notified</done>
</task>
</tasks>

<success_criteria>
- Parser unit-tested; visual_summary/onscreen_text persisted; enrichment completes with notification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-understand-transcribe-see/02-03-SUMMARY.md`
</output>
