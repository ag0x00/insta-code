{
  "name": "reel-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted, single-user research catalog for Instagram reels (capture & ingest spine).",
  "scripts": {
    "worker": "bun run src/index.ts",
    "submit": "bun run src/intake/cli.ts",
    "migrate": "bun run src/db/migrate.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.0",
    "typescript": "^5.6.0"
  }
}
